#ifndef _MR_UTILITY
#define _MR_UTILITY

void printKey( const unsigned char* key, unsigned int size ); // 打印键值
bool cmpKey( const unsigned char* keyA, const unsigned char* keyB, unsigned int size );// 比较两个键值的大小

#endif
